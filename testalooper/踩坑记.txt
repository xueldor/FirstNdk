1、执行pthread_create后应用崩溃
--》经分析，应该是pthread_create创建的子线程执行过程中发生异常
一开始以为是ALooper_prepare报错，后来发现ALooper_prepare后面的log打印出来了
解决方法一：子线程的run方法返回void*,而代码中没有return语句。编译也能过，执行到这就崩溃了。
解决方法二：或者用pthread_exit(NULL)正常退出线程。